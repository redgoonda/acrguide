services:
  - type: web
    name: acrguide
    runtime: python
    buildCommand: pip install -e .
    startCommand: python -m acrguide.main
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
